<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="view_pc_ordenador_list" model="ir.ui.view">
        <field name="name">pc.ordenador.list</field>
        <field name="model">pc.ordenador</field>
        <field name="arch" type="xml">
            <list string="ordenadores">
                <field name="numero_equipo"/>
                <field name="user_id"/>
                <field name="precio_total"/>
            </list>
        </field>
    </record>

    <record id="view_pc_ordenador_form" model="ir.ui.view">
        <field name="name">pc.ordenador.form</field>
        <field name="model">pc.ordenador</field>
        <field name="arch" type="xml">
            <form string="ordenador">
                <sheet>
                    <group>
                        <field name="numero_equipo"/>
                        <field name="user_id"/>
                        <field name="components_ids" widget="many2many_tags"/>
                        <field name="ultima_mod"/>
                        <field name="precio_total" readonly="1"/>
                        <field name="incidencias"/>
                        <field name="os_tag_ids" widget="many2many_tags"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
